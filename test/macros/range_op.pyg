gimport macros.operator

#Упрощённое создание макросов для операторов. Здесь используется только что определённый макрос
infxl(0) '..' (b, e): return range(b, e+1)

# Тестируем макросы
def test(a,b):
    return list(a+1..10+b)

def validation(a,b):
    return list(range(a+1,b+11))

